# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
lambda_telementry_api/lambda/go/dist
lambda_telementry_api/lambda/py/dist
lambda_telementry_api/lambda/py/package
.DS_Store
lambda_telementry_api/lambda/ts/dist
.vscode
loki_extension/dist
